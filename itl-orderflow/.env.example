# =============================================================================
# ITL OrderFlow — Environment Variables
# =============================================================================
# Copy to .env and fill in your values. NEVER commit .env with real secrets.
# =============================================================================

# ── Database ──────────────────────────────────────────────────────────────────
# PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/itl_orderflow?schema=public"

# ── Authentication (Auth.js / NextAuth v5) ────────────────────────────────────
# REQUIRED — JWT signing key. Generate with: openssl rand -base64 32
AUTH_SECRET=""
# Base URL of your app (used for callbacks)
AUTH_URL="http://localhost:3000"

# ── Application ───────────────────────────────────────────────────────────────
# Public app URL (used in email links, portal links)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ── Email (Resend) — optional in dev, required for sending emails ─────────────
# Get your API key at https://resend.com
RESEND_API_KEY=""
EMAIL_FROM="ITL OrderFlow <noreply@yourdomain.com>"

# ── CRON Security — required in production ────────────────────────────────────
# Protects /api/cron/* endpoints. Generate with: openssl rand -hex 32
CRON_SECRET=""

# ── File Upload (Uploadthing) — optional, local upload used by default ────────
UPLOADTHING_SECRET=""
UPLOADTHING_APP_ID=""
